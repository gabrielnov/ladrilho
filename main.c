#include <stdio.h>
#include "read.h"

void swap(int **matrix, int row, int column, struct Movimento *movimento){
    int aux;
}

int main(){

    int **matrix, i , j, size, row, column;
    struct Movimento *movimento;
    matrix = readMatrix(&size);
    size = row = column;
    movimento = readR(&size);
    //procurar o local do 0
    //mover os ladrilhos de acordo com o moveset

    return 0;
}