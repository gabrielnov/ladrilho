#include <stdio.h>
#include "read.h"

int main(){

    int ** matrix;
    struct Movimento *movimento;
    matrix = readMatrix();
    movimento = readR();
    // printf("%d", resposta);
    return 0;
}